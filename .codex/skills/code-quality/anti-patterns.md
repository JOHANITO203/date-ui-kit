# code-quality anti-patterns

- relying only on manual visual checks for state-heavy changes.
- skipping runtime checks when touching `appRuntimeStore`.
- letting docs drift after changing conventions.
- hiding known issues instead of tracking them as explicit debt.

# responsive checklist

- identify token family and screen affected.
- inspect `useDevice` branches in target screen.
- test dense states (badges, long names, long labels).
- verify safe-area and keyboard behavior if input exists.
- validate desktop + mobile before closing.

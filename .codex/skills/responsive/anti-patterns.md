# responsive anti-patterns

- fixing clipping by shrinking text globally.
- replacing tokenized spacing with inline per-component overrides.
- introducing breakpoint constants that conflict with existing hook breakpoints.
- shipping without validating both shell modes (sidebar vs bottom nav).

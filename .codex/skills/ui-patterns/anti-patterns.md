# ui-patterns anti-patterns

- replacing semantic badge names with visual-only shorthand that harms clarity.
- inventing new gradients/token families while equivalent ones already exist.
- changing monetization labels in code without translation and docs alignment.
- treating verified as equivalent to paid status.

# business-rules checklist

- identify affected business concept (badge, entitlement, token, relation state).
- inspect domain resolver and runtime store paths first.
- ensure contract types still match behavior.
- run runtime spec check.
- verify cross-screen consistency where same rule is rendered.

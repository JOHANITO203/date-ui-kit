# animations anti-patterns

- creating animation state with uncached external snapshots (loop risk).
- coupling business state transitions to animation-only timers.
- adding pulse/glow loops that overpower primary content.
- using animation to hide unresolved layout bugs.
